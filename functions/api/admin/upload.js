import { requireAdmin } from './_auth.js';
export async function onRequestPost(context){const d=await requireAdmin(context);if(d)return d;if(!context.env.MEDIA)return json({error:'Bucket R2 MEDIA non configurato'},500);const form=await context.request.formData();const file=form.get('file');if(!(file instanceof File))return json({error:'File mancante'},400);if(file.size>5*1024*1024)return json({error:'Foto troppo grande. Massimo 5 MB.'},400);const allowed=['image/jpeg','image/png','image/webp'];if(!allowed.includes(file.type))return json({error:'Formato non supportato'},400);const ext=file.type==='image/png'?'png':file.type==='image/webp'?'webp':'jpg';const key=`products/${crypto.randomUUID()}.${ext}`;await context.env.MEDIA.put(key,file.stream(),{httpMetadata:{contentType:file.type,cacheControl:'public, max-age=31536000, immutable'}});return json({url:`/api/media/${key}`})}
const json=(x,s=200)=>new Response(JSON.stringify(x),{status:s,headers:{'Content-Type':'application/json'}});
