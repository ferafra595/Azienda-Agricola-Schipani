export async function onRequestGet({env,params}){if(!env.MEDIA)return new Response('Media non configurati',{status:404});const path=Array.isArray(params.path)?params.path.join('/'):params.path;const obj=await env.MEDIA.get(path);if(!obj)return new Response('Not found',{status:404});const headers=new Headers();obj.writeHttpMetadata(headers);headers.set('etag',obj.httpEtag);headers.set('Cache-Control','public, max-age=31536000, immutable');return new Response(obj.body,{headers})}
